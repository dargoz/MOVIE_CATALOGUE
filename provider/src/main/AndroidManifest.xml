<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dargoz.provider">
    <uses-permission android:name="com.dargoz.provider.READ_DATABASE" android:protectionLevel="normal"/>
    <uses-permission android:name="com.dargoz.provider.WRITE_DATABASE" android:protectionLevel="normal"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:authorities="com.dargoz.provider"
            android:name=".FilmProvider"
            android:exported="true"
            android:readPermission="com.dargoz.provider.READ_DATABASE"
            android:writePermission="com.dargoz.provider.WRITE_DATABASE" />
    </application>

</manifest>